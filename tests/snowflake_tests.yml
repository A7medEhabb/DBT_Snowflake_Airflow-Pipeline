version: 2

models:
  - name: stg_orders
    columns:
      - name: order_status
        data_tests:
          - accepted_values: 
              values: ['Completed' , 'Pending' , 'Cancelled']
  - name: stg_customers
    columns:
      - name: customer_id
        data_tests:
          - unique
